// Types sheet for types used in multiple places

export type Exercise = {
  id: string;
  name: string;
  weight: number;
  category: string;
  reps: number[];
  intensity: number;
  time: number;
};
